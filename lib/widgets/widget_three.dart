import 'package:flutter/widgets.dart';

class WidgetThree extends StatelessWidget {
  const WidgetThree({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return  const Center(child: Text('This is widget Three'));
  }
}