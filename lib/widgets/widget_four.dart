import 'package:flutter/widgets.dart';

class WidgetFour extends StatelessWidget {
  const WidgetFour({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(child:  Text('This is widget Four'));
  }
}